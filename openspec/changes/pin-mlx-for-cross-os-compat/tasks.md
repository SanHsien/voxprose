## 1. Pin MLX in dependency declarations

- [x] 1.1 Edit `requirements.txt` to replace any unconstrained `mlx` (or `mlx>=...` without upper bound) line with `mlx>=0.29,<0.30`; verification: `grep -nE "^mlx[<>=!]" requirements.txt` returns exactly one line matching `mlx>=0.29,<0.30` and zero unbounded `mlx` lines.
- [x] 1.2 Check whether `pyproject.toml` exists at repo root; if it does, add the same `mlx>=0.29,<0.30` pin to its `[project].dependencies` (or equivalent) and verify with `grep "mlx" pyproject.toml`. If `pyproject.toml` does not exist, leave a comment in `tasks.md` noting it was not present at change-apply time so the obligation transfers to whoever first creates it; verification: either the grep succeeds OR the absence is explicitly documented in this tasks file's "Implementation notes" section.

## 2. Pre-build MLX version guard

- [x] 2.1 Create `scripts/pre_build_check.py` that imports `mlx`, reads `mlx.__version__` (falling back to `subprocess.check_output(["pip", "show", "mlx"])` if `__version__` is missing), parses the version into a `(major, minor, patch)` tuple, and exits with status 0 only if `(major, minor) == (0, 29)`; verification: running `/Library/Frameworks/Python.framework/Versions/3.12/bin/python3.12 scripts/pre_build_check.py` against the current dev environment (which has MLX 0.29.4) prints `[pre-build] mlx 0.29.4 OK (macosx_15_0_arm64)` and exits 0.
- [x] 2.2 In the same `pre_build_check.py`, add a wheel-tag check that locates the installed mlx package's `RECORD` or `METADATA` (via `importlib.metadata.distribution("mlx").files` or `read_text("METADATA")`) and confirms it contains a string matching `macosx_15_0_arm64` or older (`macosx_14_0_arm64`, `macosx_13_0_arm64`); verification: function returns True on dev machine, and unit-style assertion in script body raises with a clear message if the tag is `macosx_26_0_arm64`.
- [x] 2.3 Edit `build_all.sh` to invoke `python3.12 scripts/pre_build_check.py` before the `python3.12 setup.py py2app` line; the build SHALL exit immediately with the script's non-zero status if the check fails; verification: temporarily upgrade to `mlx==0.31.1` in a scratch venv, run `bash build_all.sh`, confirm it aborts before py2app starts and prints the remediation command containing `pip install 'mlx==0.29.4'`.
- [x] 2.4 Verify the failure-path message format by manually calling `python3.12 scripts/pre_build_check.py` with a mocked higher version (e.g., set environment variable `_MLX_VERSION_OVERRIDE=0.31.1` and have the script honour it for testing) and confirming stderr contains both the literal version string and the literal remediation command; verification: stderr capture matches both substrings via grep.

## 3. Post-build metallib sanity check

- [x] 3.1 Add a `check_metallib(app_dir)` function to `post_build_fix.py` that locates `app_dir/Contents/Resources/lib/python3.12/lib-dynload/mlx/lib/mlx.metallib`, prints `[Post-Build Fix] mlx.metallib: <bytes> bytes, mtime <ISO-8601>` when present, and prints `[Post-Build Fix] WARNING: mlx.metallib missing at <path>` when absent; the function SHALL never raise; verification: calling `check_metallib(Path("dist/嘴炮輸入法.app"))` after a successful build prints a line containing `mlx.metallib:` followed by a byte count.
- [x] 3.2 Within `check_metallib`, attempt `subprocess.run(["xcrun", "metal-objdump", "--version-info", str(metallib_path)], capture_output=True, text=True, timeout=10)`; if the call succeeds and the captured stdout contains `4.0` or higher MSL version, print `[Post-Build Fix] WARNING: metallib appears to use MSL 4.0 (downgrade MLX to <0.30 to support macOS 15)`; if the call fails (FileNotFoundError, non-zero exit, or timeout), print `[Post-Build Fix] metallib MSL version parse skipped (metal-objdump unavailable)` and continue; verification: on a build host that has Xcode CLI tools, the parse line appears; if Xcode is uninstalled, the skip line appears.
- [x] 3.3 Wire `check_metallib(app_dir)` into `fix_bundle()` to run AFTER `fix_libssl_rpath(app_dir)` and BEFORE `reseal_bundle(app_dir)`; verification: reading `post_build_fix.py` shows the call sequence in `fix_bundle` is `fix_libssl_rpath → check_metallib → reseal_bundle → verify_seal`, with no codesign-modifying call between `check_metallib` and `reseal_bundle`.

## 4. Developer documentation

- [x] 4.1 Edit `README.md` to add (or update if a section already exists) a Build Requirements paragraph naming MLX, the pinned range `>=0.29,<0.30`, and the rationale "macOS 15 compatibility (MLX 0.30+ targets macOS 26-only Metal Shading Language 4.0)"; verification: `grep -E "mlx>=0.29|mlx==0.29.4" README.md` returns at least one match and `grep -i "macOS 15" README.md` matches the explanatory line.
- [x] 4.2 Edit root `CLAUDE.md` to add a section titled `## MLX Version Pin` (sibling of the existing `## OpenSSL Bundling` section) describing the same constraint, the same remediation command, and a sentence explaining why future agents must NOT run `pip install --upgrade mlx` without a Spectra change; verification: `grep -i "MLX Version Pin" CLAUDE.md` matches and the section body contains both `0.29.4` and the words `do not upgrade`.
- [x] 4.3 Append a one-line entry to `AI_MEMORY.md` of the form `- 2026-05-14 MLX must stay <0.30 for macOS 15 support — see openspec/specs/mlx-version-pin/ or pin-mlx-for-cross-os-compat change`; verification: `tail -5 AI_MEMORY.md` shows the new entry verbatim.

## 5. End-to-end verification on this change

- [x] 5.1 Run `bash build_all.sh` end-to-end from a clean state on the dev machine (which now has MLX 0.29.4); verification: build completes with exit 0, the pre-build guard prints its OK line, the post-build sanity check prints both the metallib byte/mtime line and either the MSL parse line or the skip line, the existing `reseal_bundle` and `verify_seal` log lines still appear in the expected order.
- [x] 5.2 Inspect the resulting `dist/嘴炮輸入法.app/Contents/Resources/lib/python3.12/lib-dynload/mlx/lib/mlx.metallib` and confirm size is non-zero and matches the source `/Library/Frameworks/Python.framework/Versions/3.12/lib/python3.12/site-packages/mlx/lib/mlx.metallib` to within metadata noise; verification: `stat -f %z` on both files returns equal byte counts.
- [x] 5.3 Force a regression scenario: in a temporary clone or worktree, run `pip install 'mlx==0.30.6'` then `bash build_all.sh` and confirm the build aborts at the pre-build guard with the documented error message and exit code; verification: shell exit status is non-zero AND the captured output contains both the version string `0.30.6` and the literal remediation command. Restore MLX 0.29.4 afterwards.
