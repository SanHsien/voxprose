## 1. Implement bundle re-seal

- [x] 1.1 Add a `reseal_bundle(app_dir)` helper in `post_build_fix.py` that invokes `codesign --force --deep --sign - --timestamp=none --options runtime <app_dir>` via `subprocess.run` and captures stdout/stderr; verification: function exists and is callable with a `Path` argument, returns a `subprocess.CompletedProcess`, prints a `[Post-Build Fix] Re-sealing entire .app bundle...` line before invocation and `[Post-Build Fix] ✓ Bundle re-sealed` on success or `[Post-Build Fix] ERROR: re-seal failed: <stderr>` on non-zero exit.
- [x] 1.2 Call `reseal_bundle(app_dir)` exactly once at the end of `fix_bundle()`, after `fix_libssl_rpath(app_dir)` and before the final `print("[Post-Build Fix] Done!")`; verification: reading the modified file shows the `reseal_bundle` call as the last operation inside `fix_bundle` before the `Done!` print, and no further `install_name_tool` or `codesign` calls appear after it.
- [x] 1.3 Add a `verify_seal(app_dir)` helper that runs `codesign --verify --deep --strict <app_dir>` and `spctl -a -vvv -t install <app_dir>`, capturing both exit codes and stderr; verification: function prints `[Post-Build Fix] verify codesign: rc=<n>` and `[Post-Build Fix] verify spctl: rc=<n> stderr=<first-line>`, and returns a tuple `(codesign_rc, spctl_stderr)` to the caller.
- [x] 1.4 Call `verify_seal(app_dir)` immediately after `reseal_bundle(app_dir)` and warn (do not raise) if `codesign --verify --deep --strict` returns non-zero or if the spctl stderr contains the substring `a sealed resource is missing or invalid`; verification: feeding a deliberately-corrupted bundle path produces a `[Post-Build Fix] WARNING: bundle still has sealed-resources issue` line and the script still exits 0.

## 2. Validate on a real build

- [x] 2.1 Run a fresh build pipeline (`python setup.py py2app` then `python post_build_fix.py dist/嘴炮輸入法.app`) and confirm `codesign --verify --deep --strict dist/嘴炮輸入法.app` returns exit code 0; verification: shell command exits 0 and prints no error.
- [x] 2.2 On the same built bundle, run `spctl -a -vvv -t install dist/嘴炮輸入法.app` and confirm the stderr does NOT contain `a sealed resource is missing or invalid`; verification: grep the stderr output — the offending phrase must be absent (the command itself may still fail with `source=Unnotarized Developer ID` or `source=No Matching Profile`, which is expected for ad-hoc signing and is not a regression).
- [x] 2.3 Independent second verification run on the same bundle without rebuilding: re-run both `codesign --verify --deep --strict` and `spctl -a -vvv -t install` to confirm the seal is stable across invocations; verification: both commands produce identical exit codes and stderr substrings on the second run.
