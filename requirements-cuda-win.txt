# CUDA acceleration libraries for faster-whisper (ctranslate2).
# Only needed on machines with an NVIDIA GPU; setup_win.bat installs this
# automatically when nvidia-smi is present. Without these the app runs on CPU.
nvidia-cublas-cu12>=12.1.3,<13
nvidia-cudnn-cu12>=8.9.2,<10
