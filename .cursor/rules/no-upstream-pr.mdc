---
description: Fork PRs must target SanHsien origin; upstream PRs need explicit contribute-back consent
alwaysApply: true
---

# 對外只打本 fork

本 repo 是 fork（上游位址見 `git remote -v` 的 `upstream`）。PR、push、release 一律指向
`SanHsien/voxprose`。

**根因是機制不是粗心：** `gh` 在 fork clone 的預設 repo 就是上游，裸跑 `gh pr create` 必然打上去。

```powershell
gh repo set-default SanHsien/voxprose    # 每個 clone 先跑一次
gh repo set-default --view             # 必須回 SanHsien/voxprose
gh pr create --repo SanHsien/voxprose --base main --head <分支>
```

建完**讀輸出的 URL**，owner 必須是 `SanHsien`。不是就立刻 `gh pr close` 留言道歉說明開錯 repo，
再對 origin 重開，並主動告訴使用者。

2026-08-22 一天內兩個工具各誤開一個上游 PR（`lidge-jun/opencodex#2373`、
`hamanpaul/paulsha-cortex#787`）。兩次都是「指令成功了」的錯覺——開錯的 owner 就寫在輸出的 URL 裡。
批次跑多個 repo 時最容易略過確認，那正是出事的場合。

**唯一例外：** 使用者在**這次對話**明確同意回貢上游。fork、建環境、開 PR、比照其他 repo、
合併回 main、「所有專案都照做」都不是例外。
